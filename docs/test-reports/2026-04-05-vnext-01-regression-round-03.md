# 2026-04-05 VNext-01 缺陷回归测试报告 Round 03

## 回测状态

- 回测状态：复测失败
- 回测结论：基于 `docs/test-reports/2026-04-04-vnext-01-bugfix-round-01.md` 执行新一轮真实页面回归后，`BUG-VNEXT01-01` 仍未修复。新增对象与新增页面都会成功写入文档和本地存储，顶部 `撤销 / 重做` 按钮却继续同时保持禁用，用户仍无法实际使用 `VNext-01`。
- 对应初测报告：`docs/test-reports/2026-04-04-vnext-01-round-01.md`
- 对应修复文档：`docs/test-reports/2026-04-04-vnext-01-bugfix-round-01.md`

## 回测范围

- `BUG-VNEXT01-01` 撤销 / 重做按钮可用性
- 与该缺陷直接相关的最小关联链路：
  - 新增 `Text` 后 `撤销 / 重做` 是否立即可用
  - 新增 `Text` 后自动保存恢复为 `已保存` 时按钮状态是否被错误重置
  - 新增 `Slide` 后 `撤销 / 重做` 是否立即可用
  - 当按钮仍被禁用时，本轮不再继续展开实际撤销 / 重做动作验证

## 回测环境

- 回测日期：2026-04-05
- 仓库路径：`/Users/admin/MyDemo/canvas-courseware`
- 本地访问地址：`http://127.0.0.1:32173/projects`
- 操作系统：`Darwin 25.3.0 arm64`
- 浏览器执行环境：`Playwright 1.58.0`（Python chromium）
- Python：`3.13.12`
- Node.js：`v24.14.0`
- pnpm：`10.30.3`
- 说明：按当前 `AGENTS.md` 约定，本轮回测未执行 `typecheck` / `build`

## 回测结果

### BUG-VNEXT01-01 撤销 / 重做按钮仍禁用

- 回测状态：复测失败
- 执行路径 1：
  1. 进入项目列表页
  2. 点击“新建项目”
  3. 点击“文本”新增对象
  4. 等待自动保存恢复为 `已保存`
  5. 观察顶部 `撤销 / 重做` 按钮状态
- 回测结果 1：
  - 当前项目首个 slide 的节点数已变为 `1`
  - `撤销` 仍为禁用
  - `重做` 仍为禁用
  - 因为 `撤销` 没有开放，本轮无法继续执行真实的撤销 / 重做闭环
- 执行路径 2：
  1. 返回项目列表页
  2. 再次点击“新建项目”
  3. 点击 `+ 新建页面`
  4. 等待自动保存恢复为 `已保存`
  5. 观察顶部 `撤销 / 重做` 按钮状态
- 回测结果 2：
  - 当前项目页数已变为 `2`
  - `撤销` 仍为禁用
  - `重做` 仍为禁用

### 关键观察

- 当前真实页面表现与 `2026-04-05 Round 02` 保持一致，没有看到任何用户可感知修复结果。
- 两条路径都证明“新增对象 / 新增页面命令本身”和“自动保存落盘”仍然正常，问题继续收敛在历史状态同步到顶部按钮禁用态的链路上。
- 由于最基础的 `撤销` 入口仍未对用户开放，`VNext-01` 的产品 review 继续阻塞。

## 证据文件

- `docs/test-reports/assets/2026-04-05-vnext-01-regression-round-03/after-add-text.png`
- `docs/test-reports/assets/2026-04-05-vnext-01-regression-round-03/after-add-slide.png`

## 未覆盖范围

- 真实点击 `撤销 / 重做` 后的文档恢复链路未继续展开，因为按钮仍未开放
- 属性修改、拖拽、缩放、图层顺序、slide 管理和时间轴编辑的撤销 / 重做未继续展开，因为基础入口仍被阻塞
- 快捷键撤销 / 重做未测试；该项属于 `VNext-02` 范围，不在本轮回测范围内

## 剩余风险

- 当前 `VNext-01` 对用户仍不可用，产品 review 无法通过。
- 建议开发下一步直接在真实页面里继续核对顶部历史按钮的 `disabled` 绑定、`historyState` 响应式依赖和保存后重新渲染时机，而不只停留在最小脚本验证。
