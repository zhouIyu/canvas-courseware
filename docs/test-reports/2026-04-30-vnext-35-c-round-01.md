# 2026-04-30 VNext-35-C Round 01 测试报告

## 测试范围

- `VNext-35-C` 时间轴编排效率优化
- 步骤列表头部触发方式与目标对象摘要
- 单步折叠 / 展开与全局折叠 / 展开
- 基于当前步骤“后插一步”快速新增
- 既有步骤复制链路
- 时间轴清理回归（联动 `VNext-22`）

## 测试环境

- 测试日期：2026-04-30
- 测试方式：自动化回归
- 执行命令：`pnpm test:vnext-35-c`、`pnpm test:vnext-22`
- 本地地址：`http://127.0.0.1:32173/projects`
- 浏览器：Playwright Chromium Headless
- 视口：`1440 x 900`

## 测试结果

- 通过：时间轴步骤头部可直接展示 `页面点击 / 对象点击 / 自动触发` 等触发方式摘要，并同时回显触发对象、目标对象与动画类型。
- 通过：单步“收起 / 展开”后，步骤头部摘要仍保留，便于在折叠态下继续识别当前编排关系。
- 通过：头部“全部收起 / 全部展开”可稳定切换所有步骤卡片状态。
- 通过：对“点击插图继续”执行“后插一步”后，步骤会插入到当前步骤后方，并继承原步骤的触发方式、目标对象与动作结构，同时使用新的步骤与动作 id。
- 通过：既有“复制”入口仍可正常生成 `副本` 步骤，触发方式与动作配置保持一致。
- 通过：联动回归 `VNext-22` 后，删除节点、批量删除、设为背景与预览消费清理后 timeline 的链路均保持通过，未发现时间轴清理回归。
- 通过：两轮自动化均未采集到控制台错误或页面运行时错误。

## 结论

- 本轮测试未发现缺陷，`VNext-35-C` 当前可标记为“复测通过”。
- `TimelinePanel` 已拆分动作编辑子组件，当前时间轴面板复杂度已从超长单文件收敛到可维护范围。
- 相关结构化结果见 [run-summary.json](/Users/admin/MyDemo/canvas-courseware/docs/test-reports/assets/2026-04-30-vnext-35-c-round-01/run-summary.json)。
- 相关截图见 [timeline-efficiency-panel.png](/Users/admin/MyDemo/canvas-courseware/docs/test-reports/assets/2026-04-30-vnext-35-c-round-01/timeline-efficiency-panel.png)。

## 未覆盖范围与剩余风险

- 本轮未覆盖更大规模的多步骤长列表场景，例如单页十几个步骤时的滚动与摘要可读性。
- 本轮未覆盖预览态沉浸式播放控制，因为该能力属于后续 `VNext-35-D` 范围。
- 如果后续继续扩展时间轴批量操作，建议补充拖拽排序与多 slide 切换后的折叠态持久化专项回归。
