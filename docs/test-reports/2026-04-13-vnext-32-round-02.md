# 2026-04-13 VNext-32 Round 02 测试报告

## 测试范围

- `BUG-2026-04-13 VNext-32-A` 工作台创建后崩溃复测
- 新建项目进入工作台后的首屏渲染链路
- 顶部 `项目标题` 输入框、编辑器壳层、预览壳层与三栏主布局的基础 UI 表现
- 基础冒烟链路：插入文本、自动保存 / 手动保存、编辑态切到预览态、再切回编辑态
- `1440 x 900` 视口下编辑态与预览态是否出现非预期横向滚动

## 测试环境

- 测试时间：2026-04-13
- 运行方式：Google Chrome Headless 自动化回归
- Chrome 可执行文件：`/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`
- 访问地址：`http://127.0.0.1:32173/projects`
- 视口：`1440 x 900`
- 前置条件：本地开发服务已启动

## 测试结果

- 结论：通过
- 本轮复用了并更新 `test/vnext-32/round-01.mjs`，把旧脚本中基于 `input[aria-label="项目标题"]` 的过时断言，调整为符合当前 Arco Input 实际 DOM 结构的校验方式，并补齐进入工作台后的基础冒烟步骤。
- 新建项目后进入工作台未再出现 `TypeError: Cannot set properties of null (setting 'value')`，顶部 `项目标题` 输入框、工作台顶栏和编辑器壳层均已正常渲染。
- 编辑态下通过顶部工具栏插入文本后，新增节点已成功写入本地存储；随后手动点击“保存”，页面状态回到“已保存”。
- 从编辑态切换到预览态后，预览壳层、左侧页面导航、中央预览画布区和右侧步骤状态区均已正常渲染；再切回编辑态后，编辑器主工作区可继续使用。
- 编辑态与预览态在 `1440 x 900` 视口下的 `horizontalOverflow` 均为 `false`，本轮未观察到非预期横向滚动条、明显截断或布局塌陷。
- 本轮自动化未捕获新的控制台错误或页面运行时异常。

## 缺陷复测结论

### 缺陷 1：新建项目进入工作台后页面崩溃

- 问题标题：`BUG-2026-04-13 VNext-32-A` 工作台创建后崩溃
- 当前状态：复测通过
- 复测结果：
  1. 新建项目后可正常进入 `/projects/<projectId>?mode=edit`。
  2. 顶部 `项目标题` 输入框可见且可读取默认值 `未命名课件`。
  3. 编辑器首屏主链路已恢复，插入文本、保存、切换预览和返回编辑均可继续执行。
  4. 未再复现首屏挂载阶段对 `null.value` 赋值导致的崩溃。

## 测试证据

- 自动化脚本：`test/vnext-32/round-01.mjs`
- 结构化结果：`docs/test-reports/assets/2026-04-13-vnext-32-round-02/run-summary.json`
- 编辑态截图：`docs/test-reports/assets/2026-04-13-vnext-32-round-02/workspace-entry.png`
- 预览态截图：`docs/test-reports/assets/2026-04-13-vnext-32-round-02/preview-entry.png`
- 对应修复文档：`docs/test-reports/2026-04-13-vnext-32-workspace-crash-fix-round-01.md`

## 未覆盖范围与剩余风险

- 本轮聚焦于 `VNext-32-A` 的首屏崩溃回归与基础编辑主链路，未继续深入覆盖刷新恢复、复杂 Fabric 交互或更长链路的时间轴配置场景。
- 本轮测试未执行 `pnpm typecheck` 或 `pnpm build`；静态基线检查仍属于开发 / 修复角色的交付前自查范围。
