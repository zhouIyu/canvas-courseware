# 2026-04-06 VNext-07 回归测试报告 Round 02

## 回测状态

- 回测状态：通过
- 回测结论：基于 `docs/test-reports/2026-04-06-vnext-07-bugfix-round-01.md` 提供的修复说明继续执行真实页面回归后，工作台内嵌预览态“重置播放”入口已复测通过；同时补充验证了步骤重命名、步骤删除，以及动画 `duration / delay` 与自动步骤 `delay` 配置链路。结合 Round 01 已通过的步骤排序、步骤复制与“从当前步骤开始预览”，`VNext-07` 当前范围已完成产品回归。
- 对应初测报告：`docs/test-reports/2026-04-06-vnext-07-round-01.md`
- 对应修复文档：`docs/test-reports/2026-04-06-vnext-07-bugfix-round-01.md`

## 回测范围

- 工作台内嵌预览态“重置播放”入口可见性与实际重置效果
- 重置后再次推进步骤的可重复调试链路
- 步骤重命名
- 步骤删除
- 动画 `duration / delay` 参数配置与保存链路
- 自动步骤 `delay` 参数保存链路

## 回测环境

- 回测日期：2026-04-06
- 仓库路径：`/Users/admin/MyDemo/canvas-courseware`
- 本地访问地址：`http://127.0.0.1:32173/projects`
- 浏览器执行环境：`Playwright 1.58.0`（Python chromium）
- 说明：按当前 `AGENTS.md` 约定，本轮回测未执行 `typecheck` / `build`
- 说明：开发侧已在修复文档中记录 `pnpm typecheck` 与 `pnpm build` 通过，本轮测试侧仅执行真实页面回归

## 回测结果

### 回测 1：内嵌预览态“重置播放”入口

- 回测状态：复测通过
- 执行路径：
  1. 新建空白项目，新增 `3` 个文本对象并命名为“标题”“章节说明”“课堂提问”
  2. 将“章节说明”“课堂提问”设为“预览隐藏”
  3. 在“章节说明”对象上创建动画，并设置 `duration=700ms`、`delay=120ms`
  4. 配置步骤“显示章节说明（重命名）”与“显示课堂提问（自动）”
  5. 在第 2 步点击“从此预览”
  6. 进入工作台内嵌预览模式后，检查当前画布区顶部右侧
  7. 点击“重置播放”
- 回测结果：
  - 内嵌预览态顶部右侧可见明确的“重置播放”按钮
  - 从第 2 步切入预览后，步骤栏显示第 1 步已完成、当前步骤为“显示课堂提问（自动）”
  - 点击“重置播放”后，步骤状态恢复为首步当前态，第 1 步完成标记被清空

### 回测 2：重置后可再次推进步骤

- 回测状态：复测通过
- 执行路径：
  1. 在完成“重置播放”后，直接点击预览画布区域，触发当前 `page-click` 步骤
  2. 观察步骤状态区域变化
- 回测结果：
  - 点击预览画布后，第 1 步重新进入“已完成”
  - 当前步骤再次切换到“显示课堂提问（自动）”
  - 说明补齐“重置播放”入口后，不会破坏原有“从当前步骤开始预览”的重复调试链路

### 回测 3：步骤重命名与删除

- 回测状态：复测通过
- 执行路径：
  1. 将首个步骤命名为“显示章节说明（重命名）”
  2. 将第二个步骤命名为“显示课堂提问（自动）”
  3. 复制第二个步骤，确认生成“显示课堂提问（自动） 副本”
  4. 删除该副本步骤
  5. 保存项目并读取本地存储
- 回测结果：
  - 最终步骤列表保留 `2` 条：
    - `显示章节说明（重命名）`
    - `显示课堂提问（自动）`
  - 删除副本后，时间轴步骤数量与顺序写回正确

### 回测 4：动画与自动步骤参数链路

- 回测状态：复测通过
- 执行路径：
  1. 在“组件属性”中为“章节说明”对象新增一条动画
  2. 设置动画 `duration=700ms`、`delay=120ms`
  3. 将“显示课堂提问（自动）”配置为 `auto`，并设置 `delay=3000ms`
  4. 保存项目并读取本地存储
- 回测结果：
  - 动画参数正确写回为：
    - `durationMs = 700`
    - `delayMs = 120`
  - 自动步骤参数正确写回为：
    - `trigger.type = auto`
    - `trigger.delayMs = 3000`
  - 首个步骤动作正确关联到“章节说明”节点及其动画资源

## 关键观察

- 这轮修复只补了内嵌预览态入口，但入口补齐后，底层 `resetPreview` 能力在正式工作台中的行为已经恢复正常。
- 重置播放后的步骤状态回退与再次推进都稳定，说明新入口与现有播放器状态机之间没有引入新的联动回归。
- 本轮补测的步骤重命名、删除和动画 / 自动延迟参数链路也都通过，说明 `VNext-07` 当前范围在编辑态与预览态之间已经形成闭环。

## 证据文件

- `docs/test-reports/assets/2026-04-06-vnext-07-regression-round-02/inspector-animation-config.png`
- `docs/test-reports/assets/2026-04-06-vnext-07-regression-round-02/timeline-rename-delete.png`
- `docs/test-reports/assets/2026-04-06-vnext-07-regression-round-02/preview-before-reset.png`
- `docs/test-reports/assets/2026-04-06-vnext-07-regression-round-02/preview-after-reset.png`
- `docs/test-reports/assets/2026-04-06-vnext-07-regression-round-02/preview-after-replay.png`
- `docs/test-reports/assets/2026-04-06-vnext-07-regression-round-02/storage-after-timeline.json`

## 未覆盖范围

- 本轮未覆盖对象点击触发步骤
- 本轮未覆盖跨 slide 场景下的“从当前步骤开始预览”
- 本轮未覆盖动画 `duration / delay` 在真实播放时序上的精确时间测量，仅验证配置写回与回放链路可用

## 剩余风险

- 当前 `VNext-07` 的主要产品链路已回归通过，但如果后续继续调整播放器壳层、步骤触发器或时间轴表单结构，建议优先重新回归本轮的“重置播放 + 重新推进”路径。
