# 2026-04-14 VNext-32 Round 03 测试报告

## 测试范围

- `VNext-32-C` Schema 解析 / 校验 / 迁移层治理的用户可见入口回归
- 现有 `pnpm test:vnext-32` 工作台主链路冒烟回归
- JSON 导出链路
- 合法 JSON 导入后的标题、文本内容与本地持久化写回
- 非法时间轴引用导入拦截
- 不受支持 schema 版本导入拦截

## 测试环境

- 测试时间：2026-04-14 11:45 CST
- 运行方式：
  - `pnpm test:vnext-32`
  - `node test/vnext-32/round-02.mjs`
- 浏览器：Google Chrome Headless
- Chrome 可执行文件：`/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`
- 访问地址：`http://127.0.0.1:32173/projects`
- 视口：`1440 x 900`
- 前置条件：本地开发服务已启动，仓库依赖已安装

## 测试结果

- 结论：通过
- `pnpm test:vnext-32` 已通过，确认新建项目进入工作台、插入文本、自动保存 / 手动保存、编辑态切换预览态再切回编辑态的基础主链路仍然稳定。
- 新增 `test/vnext-32/round-02.mjs` 已通过，补齐 `document-io` 重构后的 JSON 导入 / 导出回归。
- 导出 JSON 成功落盘为 `未命名课件-2026-04-14.json`，导出文档 `version` 为 `1.0.0`，首轮导出 slide 数量为 `1`。
- 导入合法夹具 `import-valid.json` 后，工作台顶部项目标题已更新为 `VNext-32 导入校验课件`，首屏文本内容已更新为 `这段文本来自 round-02 JSON 导入回归。`，并成功写回当前项目本地存储。
- 导入非法时间轴引用夹具 `import-invalid-reference.json` 时，页面给出明确错误文案：`document.slides[0].timeline.steps 中的动作 action-invalid-reference 指向了不存在的节点 missing-node-for-import。`；导入失败后，当前项目标题与文本内容保持为上一次合法导入结果，未被脏数据覆盖。
- 导入旧版本夹具 `import-unsupported-version.json` 时，页面给出明确错误文案：`暂不支持 schema 版本 0.9.0，当前仅支持 1.0.0，且没有可用的迁移路径。`；导入失败后，本地项目状态同样保持不变。
- 本轮未捕获新的页面运行时异常。控制台中仅出现两条预期内的 `project.import.failed` 诊断日志，对应两次负向导入校验。

## 缺陷结论

- 本轮未发现新增缺陷。

## 测试证据

- 既有冒烟脚本：`test/vnext-32/round-01.mjs`
- 新增导入 / 导出回归脚本：`test/vnext-32/round-02.mjs`
- 结构化结果：`docs/test-reports/assets/2026-04-14-vnext-32-round-03/run-summary.json`
- 导出样本：`docs/test-reports/assets/2026-04-14-vnext-32-round-03/exported-courseware.json`
- 合法导入截图：`docs/test-reports/assets/2026-04-14-vnext-32-round-03/valid-import.png`
- 非法引用拦截图：`docs/test-reports/assets/2026-04-14-vnext-32-round-03/invalid-reference-feedback.png`
- 旧版本拦截图：`docs/test-reports/assets/2026-04-14-vnext-32-round-03/unsupported-version-feedback.png`

## 未覆盖范围与剩余风险

- 本轮验证了“当前版本直通”和“无迁移链路时明确报错”两类路径，但由于仓库中暂未注册真实的 schema 迁移步骤，尚未覆盖“旧版本成功迁移到当前版本”的正向迁移链路。
- 本轮未覆盖损坏 JSON 文本、超大文件导入、复杂多页时间轴组合文档等更重场景。
- 按测试角色约定，本轮未执行 `pnpm typecheck` 或 `pnpm build`；静态基线检查仍需由开发 / 修复角色在交付前补齐。
