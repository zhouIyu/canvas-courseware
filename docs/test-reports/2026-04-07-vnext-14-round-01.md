# 2026-04-07 VNext-14 测试报告 Round 01

## 测试状态

- 测试状态：通过
- 测试结论：`VNext-14` 的缩略图自动截图同步主链路已跑通。当前页在保存后会生成真实缩略图；切页前也会先截取当前页封面并写回项目级缓存；编辑态左侧页面栏、预览态左侧页面栏、项目列表封面与刷新恢复链路都能消费同一份持久化缩略图数据。本轮产品 review 通过。
- 对应任务：`docs/TODO.md` 中 `VNext-14 缩略图自动截图同步：已在保存与切页前补齐当前 slide 自动截图，并同步更新编辑/预览左侧缩略页封面与项目列表封面`
- 缺陷数量：0 条

## 测试范围

- 当前页保存时自动截图并写入 `slideThumbnails`
- 同一页再次编辑后的缩略图更新
- 从第二页切回第一页前的缩略图截取与持久化
- 编辑态左侧页面栏缩略页封面展示
- 预览态左侧页面栏缩略页封面展示
- 项目列表封面同步
- 刷新后缩略图恢复

## 测试环境

- 测试日期：2026-04-07
- 仓库路径：`/Users/admin/MyDemo/canvas-courseware`
- 本地访问地址：`http://127.0.0.1:32173/projects`
- 浏览器执行环境：`Playwright 1.59.0`（Node.js chromium）
- 视口：`1440 x 900`
- 自动化脚本：`pnpm test:vnext-14`
- 说明：按当前 `AGENTS.md` 约定，本轮测试未执行 `pnpm typecheck` / `pnpm build`

## 测试结果

### 测试 1：保存后自动生成首页缩略图

- 测试状态：通过
- 执行路径：
  1. 清空浏览器 `localStorage`
  2. 新建空白项目并改名为 `VNext-14 缩略图测试 Round01`
  3. 在第一页插入一个文本节点
  4. 等待自动保存完成
- 测试结果：
  - 首页 `slideThumbnails[firstSlideId]` 已生成图片 data URL
  - 项目级 `thumbnail` 也同步变为图片 data URL
  - 项目级封面与首页缩略图值一致
  - 编辑态左侧第一页缩略页开始使用真实背景图，而不是纯占位样式

### 测试 2：同一页再次编辑后会刷新缩略图

- 测试状态：通过
- 执行路径：
  1. 在第一页继续新增一个矩形节点
  2. 等待自动保存完成
  3. 对比新增矩形前后的首页缩略图 data URL
- 测试结果：
  - 首页缩略图长度从 `1803` 变为 `2291`
  - 前后两次缩略图 data URL 不一致，说明保存时重新抓取了最新画布
  - 编辑态第一页卡片背景图同步更新

### 测试 3：切页前会先截取当前页封面

- 测试状态：通过
- 执行路径：
  1. 通过第一页的“后插入”创建第二页
  2. 在第二页新增一个矩形节点
  3. 在自动保存触发前立即点击第一页，切回首页
  4. 等待自动保存完成并读取本地存储
- 测试结果：
  - 第二页 `slideThumbnails[secondSlideId]` 已生成图片 data URL
  - 第二页缩略图与第一页缩略图不同，说明不是复用了首页封面
  - 编辑态第二页卡片背景图同步可见
  - 说明“切页前先截图再切换”的链路已生效

### 测试 4：预览态左侧缩略页消费已持久化封面

- 测试状态：通过
- 执行路径：
  1. 切换到工作台“预览”模式
  2. 检查预览态左侧两张页面卡片的背景样式
- 测试结果：
  - 第一页和第二页缩略页都使用真实背景图
  - 未出现空白缩略图或回退到默认占位的问题

### 测试 5：项目列表封面与刷新恢复

- 测试状态：通过
- 执行路径：
  1. 通过顶部返回按钮回到项目列表
  2. 定位 `VNext-14 缩略图测试 Round01` 项目卡片
  3. 检查卡片封面背景图
  4. 重新打开项目并刷新页面
  5. 再次检查编辑态两张缩略页的背景样式
- 测试结果：
  - 项目列表封面使用真实背景图
  - 项目卡片封面与持久化的首页缩略图值一致
  - 刷新后本地仍保留 `2` 张 slide 缩略图
  - 重新进入工作台后，两张页面卡片都继续显示真实封面

## 关键观察

- 当前实现不是只做了界面层回显，而是把缩略图 data URL 真正写进了项目仓库：`slideThumbnails` 与项目级 `thumbnail` 都能在 `localStorage` 中读取到。
- 编辑态、预览态和项目列表实际消费的是同一份已持久化缩略图数据，因此三处封面表现保持一致。
- “先编辑第二页，再在自动保存前切回第一页”的场景本轮已覆盖，结果表明切页前截图逻辑没有被自动保存时序覆盖掉。
- 本轮自动化执行期间未捕获到控制台错误或页面运行时错误。

## 证据文件

- `docs/test-reports/assets/2026-04-07-vnext-14-round-01/editor-after-first-text.png`
- `docs/test-reports/assets/2026-04-07-vnext-14-round-01/editor-after-first-rect.png`
- `docs/test-reports/assets/2026-04-07-vnext-14-round-01/editor-after-switch-back.png`
- `docs/test-reports/assets/2026-04-07-vnext-14-round-01/preview-overview.png`
- `docs/test-reports/assets/2026-04-07-vnext-14-round-01/project-list-after-thumbnail-sync.png`
- `docs/test-reports/assets/2026-04-07-vnext-14-round-01/editor-after-reload.png`
- `docs/test-reports/assets/2026-04-07-vnext-14-round-01/storage-after-switch.json`
- `docs/test-reports/assets/2026-04-07-vnext-14-round-01/run-summary.json`

## 未覆盖范围

- 删除页面后旧缩略图缓存清理
- 复制页面后的缩略图继承与再生成策略
- 导入 JSON 后缩略图缓存重建
- 图片背景、文本内联编辑、多选编排等复杂内容对缩略图的视觉准确性

## 剩余风险

- 当前结论覆盖的是两页普通编辑场景；若后续引入更复杂的多页批量操作，建议继续回归缩略图缓存数量与封面映射关系。
- 本轮未覆盖删除页与导入 JSON，因此这些链路的缩略图一致性仍建议在后续版本或回归测试中补测。
