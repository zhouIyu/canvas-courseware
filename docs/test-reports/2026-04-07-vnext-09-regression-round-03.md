# 2026-04-07 VNext-09 回归测试报告 Round 03

## 回测状态

- 回测状态：通过
- 回测结论：基于 `docs/test-reports/2026-04-07-vnext-09-bugfix-round-02.md` 的修复说明继续执行真实页面回归后，`VNext-09` 中“新建页面 / 后插入后编辑画布残留上一页背景图”的问题已复测通过。新建页面、后插入、快速切页、复制页面边界、缩略页、刷新恢复与预览态链路均验证通过，当前页面背景配置主链路已恢复。
- 对应初测报告：`docs/test-reports/2026-04-07-vnext-09-round-01.md`
- 对应失败回归：`docs/test-reports/2026-04-07-vnext-09-regression-round-02.md`
- 对应修复文档：`docs/test-reports/2026-04-07-vnext-09-bugfix-round-02.md`

## 回测范围

- 顶部“新建页面”入口的背景初始化行为
- 页面卡片“后插入”入口的背景初始化行为
- 快速切页下的背景一致性
- 复制页面的背景继承边界
- 缩略页背景 UI 一致性
- 刷新恢复与预览态背景渲染

## 回测环境

- 回测日期：2026-04-07
- 仓库路径：`/Users/admin/MyDemo/canvas-courseware`
- 本地访问地址：`http://127.0.0.1:32173/projects`
- 浏览器执行环境：`Playwright 1.58.0`（Python chromium）
- 说明：按当前 `AGENTS.md` 约定，本轮回测未执行 `pnpm typecheck` / `pnpm build`
- 说明：开发侧已在修复文档中记录 `pnpm typecheck` 与 `pnpm build` 通过，本轮测试侧仅执行真实页面回归
- 测试输入：`docs/test-reports/assets/2026-04-07-vnext-09-regression-round-03/vnext09-bg-portrait.png`

## 回测结果

### 回测 1：顶部“新建页面”入口

- 回测状态：复测通过
- 执行路径：
  1. 新建空白项目
  2. 在第 1 页设置背景色 `#D9FBEA`
  3. 导入本地背景图，并设置填充方式为“拉伸铺满”
  4. 点击顶部工具栏“新建页面”
- 回测结果：
  - 当前激活页名称为 `第2页`
  - 右侧页面设置 UI 正确显示：
    - `field_note = 未配置`
    - `preview_chip = 仅使用纯色背景`
    - “清空背景图”按钮为禁用态
  - 编辑画布在 `200ms` 时已为纯色背景：
    - `(100, 100) = [217, 251, 234, 255]`
    - `(640, 360) = [217, 251, 234, 255]`
  - 在 `1200ms` 时同样保持纯色，未出现旧背景图晚到覆盖
- 回测结论：
  - 顶部“新建页面”已不再出现上一页背景图残留

### 回测 2：“后插入”入口与快速切页

- 回测状态：复测通过
- 执行路径：
  1. 将第 2 页背景色改为 `#FFE7C2`
  2. 在第 1 页与第 2 页之间快速切换一次
  3. 再从第 2 页卡片操作中点击“后插入”
- 回测结果：
  - 第 2 页在快速切回后保持纯色背景：
    - `(100, 100) = [255, 231, 194, 255]`
    - `(640, 360) = [255, 231, 194, 255]`
  - “后插入”生成的第 3 页 UI 同样显示：
    - `field_note = 未配置`
    - `preview_chip = 仅使用纯色背景`
    - “清空背景图”按钮为禁用态
  - 第 3 页在 `200ms` 与 `1200ms` 两个时点均保持纯色：
    - `(100, 100) = [255, 231, 194, 255]`
    - `(640, 360) = [255, 231, 194, 255]`
- 回测结论：
  - “后插入”与快速切页场景均未再出现背景图残影

### 回测 3：复制页面边界、刷新恢复与预览态

- 回测状态：复测通过
- 执行路径：
  1. 复制第 1 页，生成 `第1页 副本`
  2. 保存项目并刷新页面
  3. 切换到预览模式后，分别验证 `第2页`、`第3页` 与 `第1页 副本`
- 回测结果：
  - `第1页 副本` 继续完整继承背景图：
    - `(100, 80) = [255, 107, 107, 255]`
    - `(100, 640) = [77, 150, 255, 255]`
  - 刷新后：
    - 第 2 页 `(100, 100) = [255, 231, 194, 255]`
    - 第 3 页 `(100, 100) = [255, 231, 194, 255]`
    - `第1页 副本` `(100, 80) = [255, 107, 107, 255]`
  - 预览态下：
    - 第 2 页 `(100, 100) = [255, 231, 194, 255]`
    - 第 3 页 `(100, 100) = [255, 231, 194, 255]`
    - `第1页 副本` `(100, 80) = [255, 107, 107, 255]`
  - 编辑态与预览态缩略页背景样式一致：
    - 第 1 页与 `第1页 副本` `backgroundImage != none`
    - 第 2 页与第 3 页 `backgroundImage = none`
- 回测结论：
  - 复制页面边界、刷新恢复、缩略页和预览态链路均保持正常

## 关键观察

- 本轮修复后，状态层与画布层重新对齐。新页面在页面设置 UI、存储数据和编辑画布三处都能同步体现“未配置背景图”的真实状态。
- `200ms` 时点就已经拿到正确像素，说明这次不仅修掉了“最终会残留”的问题，也没有留下用户可见的明显首帧闪残影。
- 复制页面仍完整保留原页面背景图，说明新建页面与复制页面的职责边界目前是清晰的。

## 证据文件

- `docs/test-reports/assets/2026-04-07-vnext-09-regression-round-03/toolbar-new-clean.png`
- `docs/test-reports/assets/2026-04-07-vnext-09-regression-round-03/create-after-clean.png`
- `docs/test-reports/assets/2026-04-07-vnext-09-regression-round-03/edit-overview.png`
- `docs/test-reports/assets/2026-04-07-vnext-09-regression-round-03/preview-overview.png`
- `docs/test-reports/assets/2026-04-07-vnext-09-regression-round-03/storage-after-regression.json`
- `docs/test-reports/assets/2026-04-07-vnext-09-regression-round-03/run-summary.json`

## 未覆盖范围

- 远程 URL 背景图地址链路
- `cover` 模式在预览态下的独立像素验证
- 页面尺寸变化与背景图适配联动
- JSON 导出后再导入背景图项目

## 剩余风险

- 当前 `VNext-09` 主链路已恢复，但如果后续继续调整编辑态 / 预览态切页时序、背景图异步加载或 Fabric 画布写回机制，建议优先回归本轮的“新建页面 + 后插入 + 快速切页”组合路径。
