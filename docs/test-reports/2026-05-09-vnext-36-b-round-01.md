# 2026-05-09 VNext-36-B Round 01 测试报告

## 测试范围

- `VNext-36-B` 时间轴编排二次提效
- 时间轴步骤拖拽排序
- 多 slide 切换后的折叠态保持
- 刷新后的折叠态恢复
- 项目级 `workspaceState` 持久化
- 时间轴区域回归
- 相邻版本自动保存与持久化回归

## 测试环境

- 测试日期：2026-05-09
- 测试方式：自动化回归
- 执行命令：
  - `pnpm typecheck`
  - `pnpm build`
  - `CW_BASE_URL=http://127.0.0.1:32174/projects pnpm test:vnext-36-b`
  - `CW_BASE_URL=http://127.0.0.1:32174/projects pnpm test:vnext-35-c`
  - `CW_BASE_URL=http://127.0.0.1:32174/projects pnpm test:vnext-36-a`
- 本地地址：`http://127.0.0.1:32174/projects`
- 浏览器：Playwright Chromium Headless
- 视口：`1440 x 900`
- 前置条件：
  - 仓库依赖已安装
  - 测试开始前已清空 `localStorage` 中的 `canvas-courseware.projects`
  - 默认 `32173` 端口被本机其他进程占用，本轮回归临时改用 `32174`

## 测试结果

- 结论：通过
- 步骤拖拽排序通过：第一页时间轴可将“展示备注”从第 3 步拖到第 1 步，界面顺序与本地持久化顺序保持一致。
- 多 slide 折叠态保持通过：第一页折叠第 1 / 3 步、第二页折叠第 2 步后，来回切页仍能分别回显各自折叠状态，不再串页污染。
- 刷新恢复通过：刷新工作台后，第一页步骤顺序与两页折叠态均能从本地 `workspaceState` 正确恢复。
- 项目级持久化通过：`workspaceState.timelineCollapsedStepIdsBySlideId` 已按 slide 分维度写入本地项目记录。
- 回归通过：既有 `VNext-35-C` 时间轴效率链路与 `VNext-36-A` 自动保存 / 本地持久化链路均在同一轮临时服务上回归通过。
- 本轮自动化未捕获新增控制台错误或页面运行时错误。

## 缺陷结论

- 本轮未发现新增缺陷，`VNext-36-B` 当前可标记为“复测通过”。

## 测试证据

- 自动化脚本：`test/vnext-36/round-02.mjs`
- 结构化结果：[run-summary.json](/Users/admin/MyDemo/canvas-courseware/docs/test-reports/assets/2026-05-09-vnext-36-b-round-01/run-summary.json)
- 刷新恢复截图：[timeline-second-slide-after-reload.png](/Users/admin/MyDemo/canvas-courseware/docs/test-reports/assets/2026-05-09-vnext-36-b-round-01/timeline-second-slide-after-reload.png)

## 未覆盖范围与剩余风险

- 本轮拖拽排序覆盖的是鼠标拖拽主路径，尚未单独补充键盘可访问性层面的排序交互测试。
- 折叠态持久化当前按项目本地 `workspaceState` 保存，尚未覆盖“导入另一份 JSON 后旧折叠态是否完全清空”的独立脚本；本轮仅通过导入链路代码审查与状态重置实现兜底。
- 默认 `32173` 端口在当前机器上被其他进程占用，后续若继续跑本地回归，建议继续通过 `CW_BASE_URL` 显式指定可用端口，或先清理占用进程。
