# 2026-04-07 VNext-15 测试报告 Round 01

## 测试状态

- 测试状态：通过
- 测试结论：`VNext-15` 组件管理面板重构主链路验证通过。图层浮层默认收起、右侧管理栏默认收起、浮层列表与文档层级顺序一致、显隐 / 锁定快捷操作可直接写回、浮层拖拽可修改层级、页面设置可通过独立抽屉入口继续使用，刷新后配置仍能恢复。本轮产品 review 通过。
- 对应任务：`docs/TODO.md` 中 `VNext-15 组件管理面板重构：已将组件列表改为画布右上角独立浮层，支持拖拽排序改层级、外化高频动作，并将右侧“页面设置”改为按需打开的独立抽屉入口`
- 缺陷数量：0 条

## 测试范围

- 图层浮层默认收起与展开入口
- 右侧管理栏默认收起
- 浮层图层列表与文档节点顺序一致性
- 浮层选中与右侧组件属性联动
- 浮层显隐 / 锁定快捷操作
- 浮层拖拽改层级
- 页面设置独立抽屉入口与页面名称修改
- 编辑态刷新恢复
- 预览态烟测

## 测试环境

- 测试日期：2026-04-07
- 仓库路径：`/Users/admin/MyDemo/canvas-courseware`
- 本地访问地址：`http://127.0.0.1:32173/projects`
- 浏览器执行环境：`Playwright 1.59.0`（Node.js chromium）
- 视口：`1440 x 900`
- 自动化脚本：`pnpm test:vnext-15`
- 说明：按当前 `AGENTS.md` 约定，本轮测试未执行 `pnpm typecheck` / `pnpm build`

## 测试结果

### 测试 1：默认布局与浮层入口

- 测试状态：通过
- 执行路径：
  1. 清空浏览器 `localStorage`
  2. 新建空白项目并进入编辑页
  3. 观察初始布局
- 测试结果：
  - 右侧管理栏默认收起
  - 右上角可见独立的图层浮层入口
  - 页面设置 icon 入口可见
  - 图层浮层初始未展开

### 测试 2：浮层列表与选中联动

- 测试状态：通过
- 执行路径：
  1. 通过工具栏依次新增文本、矩形、图片框
  2. 展开图层浮层
  3. 校验浮层列表文案与顺序
  4. 点击浮层中的文本项，再展开右侧管理栏
- 测试结果：
  - 浮层列表显示为：
    - `文本-Text`
    - `矩形-Rectangle`
    - `图片-Image`
  - 列表顺序与 `localStorage` 中第一页 `nodes[]` 顺序一致
  - 右侧管理栏只保留 `组件属性`、`时间轴` 两个标签
  - 未出现旧的“页面设置”标签
  - 通过浮层选中文本节点后，右侧可正常显示 `文本属性`

### 测试 3：显隐 / 锁定快捷操作

- 测试状态：通过
- 执行路径：
  1. 在浮层中对文本节点依次点击“预览隐藏”和“锁定”
  2. 等待自动保存
  3. 读取浏览器 `localStorage`
- 测试结果：
  - 文本节点 `visible` 已写回为 `false`
  - 文本节点 `locked` 已写回为 `true`
  - 浮层按钮状态同步切换为下一步动作：
    - 显隐按钮变为 `预览显示`
    - 锁定按钮变为 `解锁`

### 测试 4：浮层拖拽改层级

- 测试状态：通过
- 执行路径：
  1. 将浮层第一项 `文本-Text` 拖拽到最后一项之后
  2. 等待自动保存
  3. 校验浮层顺序与 `localStorage`
- 测试结果：
  - 浮层顺序更新为：
    - `矩形-Rectangle`
    - `图片-Image`
    - `文本-Text`
  - `localStorage` 中第一页节点顺序同步变为：
    - `Rectangle`
    - `Image`
    - `Text`
  - 说明浮层拖拽已通过标准命令层改写真实节点层级，而非只更新界面排序

### 测试 5：页面设置抽屉入口

- 测试状态：通过
- 执行路径：
  1. 点击右上角页面设置 icon
  2. 在抽屉中把第一页名称改为 `封面页-浮层抽屉`
  3. 等待自动保存并观察左侧页面卡片
- 测试结果：
  - 页面设置抽屉可正常打开
  - 抽屉中的页面名称修改会写回文档
  - 左侧第一页名称同步更新为 `封面页-浮层抽屉`
  - `localStorage` 中第一页 `name` 也已同步为新值

### 测试 6：预览烟测与刷新恢复

- 测试状态：通过
- 执行路径：
  1. 切换到预览模式确认页面可正常打开
  2. 再切回编辑模式并刷新页面
  3. 重新展开图层浮层并读取本地存储
- 测试结果：
  - 预览画布可正常渲染
  - 刷新后第一页名称仍为 `封面页-浮层抽屉`
  - 节点顺序仍保持 `Rectangle -> Image -> Text`
  - 被移动到最后的 `Text` 节点仍保持：
    - `visible = false`
    - `locked = true`
  - 刷新后浮层顺序也继续显示为：
    - `矩形-Rectangle`
    - `图片-Image`
    - `文本-Text`

## 关键观察

- `VNext-15` 当前实现不是单纯的 UI 重排，浮层中的显隐、锁定和拖拽排序都会真实写回到文档与本地存储。
- 右侧管理栏默认收起后，编辑首屏更聚焦；同时页面设置通过独立抽屉入口保留，未影响原有页面设置链路。
- 本轮已验证“浮层选中 -> 右侧组件属性联动”仍正常，说明组件浮层与原有属性面板没有脱节。
- 自动化执行期间未捕获到控制台报错或页面运行时异常。

## 证据文件

- `docs/test-reports/assets/2026-04-07-vnext-15-round-01/editor-after-seed.png`
- `docs/test-reports/assets/2026-04-07-vnext-15-round-01/floating-panel-open.png`
- `docs/test-reports/assets/2026-04-07-vnext-15-round-01/storage-after-quick-actions.json`
- `docs/test-reports/assets/2026-04-07-vnext-15-round-01/storage-after-reorder.json`
- `docs/test-reports/assets/2026-04-07-vnext-15-round-01/floating-panel-after-reorder.png`
- `docs/test-reports/assets/2026-04-07-vnext-15-round-01/slide-settings-drawer-open.png`
- `docs/test-reports/assets/2026-04-07-vnext-15-round-01/preview-smoke.png`
- `docs/test-reports/assets/2026-04-07-vnext-15-round-01/editor-after-reload.png`
- `docs/test-reports/assets/2026-04-07-vnext-15-round-01/run-summary.json`

## 未覆盖范围

- 多选对象场景下与浮层的联动表现
- 浮层拖拽排序后的撤销 / 重做闭环
- 浮层重命名与双击编辑链路
- 页面设置抽屉中的背景图、尺寸等更复杂字段编辑

## 剩余风险

- 当前结论覆盖的是单页三对象的典型编辑链路；若后续继续扩展浮层操作范围，建议优先补测多选、重命名和撤销 / 重做。
- 本轮仅对页面设置抽屉做了页面名称修改验证，背景配置等复杂字段建议在后续回归中继续覆盖。
