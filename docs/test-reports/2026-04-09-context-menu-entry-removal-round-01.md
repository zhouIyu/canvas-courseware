# 2026-04-09 右键菜单入口收敛回归 Round 01

## 文档状态

- 测试结论：通过
- 对应改动：移除编辑区右键菜单中的“插入图片框”入口
- 关联脚本：`pnpm test:vnext-13`、`pnpm test:vnext-15`、`pnpm test:vnext-19`、`pnpm test:vnext-20`

## 测试范围

- 空白区右键菜单中的插入项收敛
- 图层浮层依赖的图片节点创建链路
- 图片右键“设为背景”主链路
- 背景填充方式确认弹层与刷新恢复

## 测试环境

- 时间：2026-04-09
- 运行方式：本地开发服务 `http://127.0.0.1:32173/projects`
- 浏览器：Headless Chromium
- 视口：`1440 x 900`

## 测试结果

- 空白区右键菜单仍保留 `插入文本 / 插入矩形 / 插入图片 / 粘贴`，`VNext-13`、`VNext-19` 与 `VNext-20` 的运行结果均确认不再出现“插入图片框”。
- 原本依赖空图片框入口的图层浮层回归，已改为直接插入真实图片节点；`VNext-15` 继续通过，图层列表、图层排序、显隐锁定与页面设置抽屉未受影响。
- 图片右键“设为背景”仍可正常工作，`VNext-19` 与 `VNext-20` 继续通过，背景写回、填充方式确认与刷新恢复未回退。
- 所有回归执行期间未捕获控制台报错或页面级异常。

## 结论

- 右键菜单入口已进一步收敛，当前编辑区不再暴露空图片框的快捷创建入口。
- 本轮调整后，图片相关的主链路继续由“插入真实图片”“图片属性换图”“右键设为背景”和“页面设置导入背景”承接。
