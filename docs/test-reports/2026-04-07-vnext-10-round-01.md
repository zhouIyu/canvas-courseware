# 2026-04-07 VNext-10 测试报告 Round 01

## 测试状态

- 测试状态：通过
- 测试结论：`VNext-10` 首页入口与项目创建主链路验证通过。项目列表页首屏精简布局可正常加载；新建项目弹窗支持预设尺寸切换与自定义尺寸下限保护；创建时可直接指定项目名称与首个页面尺寸；创建后可直接进入工作台，编辑 / 预览切换正常；返回项目列表后，新项目摘要与本地存储数据可正确回写。
- 对应任务：`docs/TODO.md` 中 `VNext-10 项目创建与首页入口优化：已完成首页首屏精简、新建项目弹窗和项目创建时直设项目名称 / 页面尺寸的链路打通`
- 缺陷结论：本轮未发现需要开发返修的新缺陷

## 测试范围

- 项目列表页首屏加载、项目数量展示与桌面视口布局
- 新建项目弹窗默认态、尺寸预设切换与自定义尺寸下限保护
- 创建时设置项目名称与首个页面尺寸
- 创建后进入工作台的编辑态 / 预览态烟测
- 返回列表后的项目摘要回写与 `localStorage` 持久化结果

## 测试环境

- 测试日期：2026-04-07
- 仓库路径：`/Users/admin/MyDemo/canvas-courseware`
- 本地访问地址：`http://127.0.0.1:32173/projects`
- 浏览器执行环境：`Playwright 1.59.0`（Node.js chromium）
- 视口：`1440 x 900`
- 说明：按当前 `AGENTS.md` 约定，本轮测试未执行 `pnpm typecheck` / `pnpm build`

## 测试结果

### 测试 1：项目列表页首屏与默认新建弹窗

- 测试状态：通过
- 执行路径：
  1. 清空浏览器 `localStorage` 中的 `canvas-courseware.projects`
  2. 进入项目列表页，等待示例项目自动注入
  3. 点击“新建项目”打开弹窗
- 测试结果：
  - 项目列表页首屏正常展示，初始项目数为 `1`
  - `1440 x 900` 视口下 `documentElement.scrollWidth = 1440`，未出现横向溢出
  - 弹窗默认摘要正确显示：
    - `标题 = 未命名课件`
    - `尺寸 = 1280 × 720`
    - `比例 = 16:9`

### 测试 2：尺寸预设切换与竖屏项目创建

- 测试状态：通过
- 执行路径：
  1. 在新建项目弹窗中切换“经典课件”“竖屏讲解”预设
  2. 输入项目名 `VNext10 竖屏创建测试 1775560988959`
  3. 点击“创建并进入工作台”
- 测试结果：
  - 预设切换时摘要即时更新：
    - `经典课件 = 1024 × 768 · 4:3`
    - `竖屏讲解 = 1080 × 1920 · 9:16`
  - 创建后直接进入编辑态工作台，路由为 `?mode=edit`
  - 顶部项目标题输入框正确回显新名称
  - 本地存储中成功写入新项目，首个页面尺寸为：
    - `width = 1080`
    - `height = 1920`

### 测试 3：新建项目后的工作台烟测

- 测试状态：通过
- 执行路径：
  1. 在新创建的竖屏项目中停留编辑态
  2. 切换到“预览”模式
  3. 再返回项目列表页
- 测试结果：
  - 编辑态工作台 `1440 x 900` 视口下未出现横向滚动
  - 预览态可正常打开，路由切换为 `?mode=preview`
  - 内嵌预览区的“重置播放”入口与“当前画布”区域正常可见
  - 返回列表后，新项目卡片排在首位，摘要显示：
    - `页数 = 1`
    - `尺寸 = 1080 × 1920`

### 测试 4：自定义尺寸下限与空标题兜底

- 测试状态：通过
- 执行路径：
  1. 再次打开“新建项目”弹窗，切到“自定义”
  2. 输入宽度 `280`、高度 `160`
  3. 清空项目名称后点击“创建并进入工作台”
- 测试结果：
  - 自定义尺寸被正确钳制到最小值：
    - `width = 320`
    - `height = 180`
  - 弹窗摘要同步显示 `320 × 180 · 自定义画布`
  - 空标题创建后，工作台标题正确回退为 `未命名课件`
  - 本地存储中存在对应项目记录，首个页面尺寸为 `320 x 180`

### 测试 5：列表摘要回写与最终状态

- 测试状态：通过
- 执行路径：
  1. 从自定义尺寸项目返回列表
  2. 检查项目列表数量、排序与卡片信息
  3. 读取 `localStorage` 最终项目列表
- 测试结果：
  - 最终项目数为 `3`
  - 列表顶部 3 个项目标题依次为：
    - `未命名课件`
    - `VNext10 竖屏创建测试 1775560988959`
    - `古诗讲解示例`
  - 首页在 `1440 x 900` 视口下仍未出现横向溢出
  - 本轮自动化执行期间 `consoleErrors = 0`、`pageErrors = 0`

## 关键观察

- 首页列表改成卡片化后，当前桌面视口下搜索框、新建按钮、卡片摘要和操作按钮布局稳定，没有出现遮挡、重叠或异常留白导致的信息断层。
- 项目卡片在长标题场景下会换行展示，但本轮截图中未出现标题压住元信息或按钮错位的问题。
- 项目创建入口已经形成比较完整的闭环：弹窗摘要、工作台标题、列表卡片摘要和 `localStorage` 中的首个页面尺寸保持一致。

## 证据文件

- `docs/test-reports/assets/2026-04-07-vnext-10-round-01/project-list-overview.png`
- `docs/test-reports/assets/2026-04-07-vnext-10-round-01/create-modal-default.png`
- `docs/test-reports/assets/2026-04-07-vnext-10-round-01/create-modal-portrait.png`
- `docs/test-reports/assets/2026-04-07-vnext-10-round-01/workspace-portrait-edit.png`
- `docs/test-reports/assets/2026-04-07-vnext-10-round-01/workspace-portrait-preview.png`
- `docs/test-reports/assets/2026-04-07-vnext-10-round-01/create-modal-custom-min.png`
- `docs/test-reports/assets/2026-04-07-vnext-10-round-01/workspace-custom-edit.png`
- `docs/test-reports/assets/2026-04-07-vnext-10-round-01/project-list-after-custom.png`
- `docs/test-reports/assets/2026-04-07-vnext-10-round-01/storage-after-vnext10.json`
- `docs/test-reports/assets/2026-04-07-vnext-10-round-01/run-summary.json`

## 未覆盖范围

- 窄屏或移动端视口下的项目列表页与新建弹窗布局
- 搜索项目、删除项目与删除确认弹层链路
- 工作台内修改项目标题后的二次保存与列表回写
- 自定义尺寸超过常规范围时的极大画布表现

## 剩余风险

- 当前 `VNext-10` 主链路已通过，但如果后续继续调整首页信息架构、卡片样式或项目创建弹窗表单结构，建议优先回归本轮的“预设尺寸切换 + 自定义尺寸下限 + 创建后返回列表”组合路径。
