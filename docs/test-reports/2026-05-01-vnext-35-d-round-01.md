# 2026-05-01 VNext-35-D Round 01 测试报告

## 测试范围

- `VNext-35-D` 预览态沉浸式播放控制
- 预览态键盘控制播放：下一步、上一步、重播当前页
- 预览态沉浸播放开关与退出链路
- 沉浸态信息降级与轻量键盘提示
- 预览态跨页快捷键切换

## 测试环境

- 测试日期：2026-05-01
- 测试方式：自动化回归
- 执行命令：`pnpm test:vnext-35-d`
- 本地地址：`http://127.0.0.1:32173/projects`
- 浏览器：Playwright Chromium Headless
- 视口：`1440 x 900`

## 测试结果

- 通过：`pnpm test:vnext-35-d`
- 通过：`pnpm test:vnext-33-g` 联动回归
- 预览态键盘控制已验证通过：`ArrowRight / 空格 / Enter` 推进下一步，`ArrowLeft` 回退到上一步并停在当前焦点步骤，`R` 重播当前页，`PageUp / PageDown` 切换上一页 / 下一页
- 沉浸播放已验证通过：进入后自动收起左右侧辅助栏、隐藏非核心提示，仅保留播放状态与轻量键盘说明；按 `Escape` 退出后，工作台内嵌播放控制可恢复
- 当前执行环境下浏览器原生全屏 API 未实际接管窗口，自动降级为窗口内沉浸模式；对应降级链路验证通过

## 结论

- `VNext-35-D` 主链路通过自动化验证，达到提测状态。
- 本轮同时修正了工作台模式切换后焦点停留在单选框时，预览快捷键被错误屏蔽的问题。
- 既有 `VNext-33-G` 预览回归脚本已同步更新到当前内嵌预览 UI 结构，联动回归通过。

## 未覆盖范围与剩余风险

- 暂未覆盖浏览器原生全屏 API 在不同内核上的兼容差异；本轮主要验证“有全屏则优先进入、失败则降级为窗口内沉浸模式”的主链路。
- 暂未覆盖对象点击触发步骤在沉浸态下的真实点击回放；本轮主验证点是播放控制入口、键盘行为和信息降级。
