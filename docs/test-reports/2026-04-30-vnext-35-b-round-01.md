# 2026-04-30 VNext-35-B Round 01 测试报告

## 测试范围

- `VNext-35-B` 多选、框选与批量对齐基础版
- 编辑态拖拽框选
- 浮层图层面板 `Shift` 多选
- 左对齐、水平居中、顶部对齐、垂直居中四类基础批量对齐
- 对齐结果自动保存与刷新恢复

## 测试环境

- 测试日期：2026-04-30
- 测试方式：自动化回归
- 执行命令：`pnpm test:vnext-35-b`
- 本地地址：`http://127.0.0.1:32173/projects`
- 浏览器：Playwright Chromium Headless
- 视口：`1440 x 900`

## 测试结果

- 通过：拖拽框选可稳定选中目标两张矩形卡片，未误选第三张卡片。
- 通过：浮层图层面板支持单击选中后，再通过 `Shift` 点击第二个图层完成多选，标准 `selection.nodeIds` 与预期一致。
- 通过：左对齐按钮可将两张卡片的 `x` 坐标对齐到同一左边界。
- 通过：水平居中按钮可将两张卡片的水平中心点对齐。
- 通过：顶部对齐按钮可将两张卡片的 `y` 坐标对齐到同一顶部边界。
- 通过：垂直居中按钮可将两张卡片的垂直中心点对齐。
- 通过：批量对齐后的几何结果可自动保存，刷新工作台后持久化位置保持一致。
- 通过：本轮自动化未采集到控制台错误或页面运行时错误。

## 结论

- 本轮测试未发现缺陷，`VNext-35-B` 当前可标记为“复测通过”。
- 相关结构化结果见 [summary.json](/Users/admin/MyDemo/canvas-courseware/docs/test-reports/assets/2026-04-30-vnext-35-b-round-01/summary.json)。
- 相关截图见 [after-marquee-selection.png](/Users/admin/MyDemo/canvas-courseware/docs/test-reports/assets/2026-04-30-vnext-35-b-round-01/after-marquee-selection.png) 和 [after-batch-alignments.png](/Users/admin/MyDemo/canvas-courseware/docs/test-reports/assets/2026-04-30-vnext-35-b-round-01/after-batch-alignments.png)。

## 未覆盖范围与剩余风险

- 本轮未覆盖多选后的分布类操作，因为 `VNext-35-B` 当前验收范围只要求基础批量对齐。
- 本轮未覆盖预览态与时间轴联动，因为该版本点只涉及编辑态选择与排版效率。
- 若后续继续扩展框选与多选到更多入口，建议补充不同缩放倍率下的回归场景。
