# 2026-04-05 VNext-01 缺陷回归测试报告 Round 02

## 回测状态

- 回测状态：复测失败
- 回测结论：基于 `docs/test-reports/2026-04-04-vnext-01-bugfix-round-01.md` 执行回归后，`BUG-VNEXT01-01` 仍未修复。新增对象与新增页面都已成功写入文档和本地存储，但顶部 `撤销 / 重做` 按钮依旧同时保持禁用。
- 对应初测报告：`docs/test-reports/2026-04-04-vnext-01-round-01.md`
- 对应修复文档：`docs/test-reports/2026-04-04-vnext-01-bugfix-round-01.md`

## 回测范围

- `BUG-VNEXT01-01` 撤销 / 重做按钮可用性
- 与该缺陷直接相关的最小关联链路：
  - 新增 `Text` 后 `撤销` 是否立即可用
  - 新增 `Slide` 后 `撤销` 是否立即可用
  - 自动保存完成后按钮状态是否被错误重置

## 回测环境

- 回测日期：2026-04-05
- 仓库路径：`/Users/admin/MyDemo/canvas-courseware`
- 本地访问地址：`http://127.0.0.1:32173/projects`
- 操作系统：`Darwin 25.3.0 arm64`
- 浏览器执行环境：`Playwright 1.58.0`
- Python：`3.13.12`
- Node.js：`v24.14.0`
- pnpm：`10.30.3`
- 说明：按当前 `AGENTS.md` 约定，本轮回测未执行 `typecheck` / `build`

## 回测结果

### BUG-VNEXT01-01 撤销 / 重做按钮仍禁用

- 回测状态：复测失败
- 执行路径 1：
  1. 进入项目列表页
  2. 点击“新建项目”
  3. 点击“文本”新增对象
  4. 等待自动保存恢复为 `已保存`
  5. 观察顶部 `撤销 / 重做` 按钮状态
- 回测结果 1：
  - 文档节点数已变为 `1`
  - `撤销` 仍为禁用
  - `重做` 仍为禁用
- 执行路径 2：
  1. 进入项目列表页
  2. 点击“新建项目”
  3. 点击 `+ 新建页面`
  4. 等待自动保存恢复为 `已保存`
  5. 观察顶部 `撤销 / 重做` 按钮状态
- 回测结果 2：
  - 文档页数已变为 `2`
  - `撤销` 仍为禁用
  - `重做` 仍为禁用

### 关键观察

- 当前问题与 Round 01 初测保持一致，说明本轮“代码链路自查已打通”的结论未在真实页面中体现出来。
- 从用户视角看，`VNext-01` 仍然等价于“历史按钮不可用”，因此产品 review 继续阻塞。

## 证据文件

- `docs/test-reports/assets/2026-04-05-vnext-01-regression-round-02/undo-still-disabled-after-add-node.png`
- `docs/test-reports/assets/2026-04-05-vnext-01-regression-round-02/undo-still-disabled-after-add-slide.png`

## 未覆盖范围

- 撤销后重做是否恢复文档状态未继续展开，因为最基础的 `撤销` 入口仍被阻塞
- 属性修改、拖拽、缩放、图层顺序、slide 管理和时间轴编辑的撤销 / 重做未继续展开，因为最基础的 `撤销` 入口仍被阻塞
- 快捷键撤销 / 重做未测试；该项属于 `VNext-02` 范围，不在本轮回测范围内

## 剩余风险

- 当前 `VNext-01` 对用户仍不可用，review 无法通过。
- 下一步建议开发直接在真实页面里核对按钮的 `disabled` 绑定和 `historyState` 刷新时机，而不只停留在最小脚本验证。
