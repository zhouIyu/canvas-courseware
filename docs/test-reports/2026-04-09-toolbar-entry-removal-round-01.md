# 2026-04-09 工具栏入口收敛回归 Round 01

## 文档状态

- 测试结论：通过
- 对应改动：移除编辑器工具栏中的“设为背景”和“图片框”入口，保留“文本 / 矩形 / 图片”
- 关联脚本：`pnpm test:vnext-11`、`pnpm test:vnext-12`、`pnpm test:vnext-15`、`pnpm test:vnext-16`、`pnpm test:vnext-19`、`pnpm test:vnext-20`

## 测试范围

- 工具栏插入区文案与入口收敛
- 图片导入、换图、属性面板回写
- 页面设置中的背景导入链路
- 页面背景独立性与刷新恢复
- 图片右键“设为背景”与填充方式确认弹层
- 图层浮层依赖的图片框创建链路

## 测试环境

- 时间：2026-04-09
- 运行方式：本地开发服务 `http://127.0.0.1:32173/projects`
- 浏览器：Headless Chromium
- 视口：`1440 x 900`

## 测试结果

- 工具栏插入区已收敛为 `文本 / 矩形 / 图片`，`VNext-11` 与 `VNext-20` 的运行结果中都确认不再出现“设为背景”和“图片框”按钮。
- 原本依赖工具栏“设为背景”的背景导入回归，已改由页面设置抽屉执行，`VNext-12` 与 `VNext-16` 继续通过，背景写回、页面独立性、刷新恢复和预览态未回退。
- 原本依赖工具栏“图片框”的测试链路，已改为通过空白区右键菜单插入图片框；`VNext-15` 与 `VNext-19` 通过，说明图层浮层、空图片框行为和右键设背景能力保持正常。
- 图片右键“设为背景”与填充方式确认弹层仍可正常工作，`VNext-19` 与 `VNext-20` 均通过。
- 所有回归执行期间未捕获控制台报错或页面级异常。

## 结论

- 本轮产品调整已完成落地，工具栏入口减少后，现有图片编辑、背景配置、右键菜单和图层管理主链路均保持可用。
- 下一步可继续基于当前交互收敛结果做体验 review，再决定是否推进 `VNext-21`。
