# Canvas Courseware VNext Plan

本文档用于记录下一版本的规划、范围边界和开发批次，不直接替代当前执行中的 `docs/TODO.md`。

## 版本定位

- 当前需求池中已进入版本计划的事项均已完成并转出 `docs/REQUIREMENTS.md`。
- 下一版本先聚焦唯一已明确、未完成的遗留问题 `BUG-05`，完成文字对象缩放链路的重新评估与修复。
- 在没有新增需求池条目的前提下，本轮版本不主动扩展新的功能范围。
- 下一版本继续遵守 UI 层与事件层解耦原则，不引入新的引擎耦合型实现。

## 核心功能范围

- [ ] BUG-05 文本对象缩放修复
  说明：
  重新评估 `Text` 对象缩放未生效问题，完成编辑态交互、属性回写、预览渲染与本地持久化链路修复。

## 暂不纳入下一版本

- 富文本
- 表格、图表、音视频
- 组合 / 打散
- 协同编辑
- 模板市场
- 图片 / PDF 导出
- 多引擎同时接入
- 新的功能型需求：待重新写入 `docs/REQUIREMENTS.md` 并完成评审后，再纳入后续版本规划

## 建议实施批次

### 第一批：遗留问题收口

- [ ] BUG-05 文本对象缩放修复
