# Canvas Courseware Execution TODO

本文档只记录当前执行中的开发、修复、联调与复测任务，不记录需求池或版本规划。每完成一项，就立即更新一次状态。

## 已完成

- [x] 明确 MVP 基本范围：Text / Image / Rect、多页 slide、每页独立 canvas
- [x] 明确交付形态：TypeScript SDK + Vue 3 monorepo + Fabric MVP
- [x] 增加架构约束：UI 层与事件层解耦
- [x] 明确编辑态与预览态的元素展示策略：编辑态默认全显，预览态按 timeline 严格逐步展示
- [x] 定义通用 JSON schema 与事件协议
- [x] 设计 `Document / Slide / Node / Timeline / Animation` schema
- [x] 设计命令接口：新增、更新、删除、选中、层级调整、切换 slide
- [x] 设计并实现编辑态 store 与控制器
- [x] 实现 `packages/core` 的 schema / reducer / store / controller 源码
- [x] 搭建 monorepo 基础工程
- [x] 搭建 `apps/playground` 初始骨架
- [x] 实现 `packages/fabric` 的编辑态适配器
- [x] 实现 `packages/fabric` 的播放态适配器
- [x] 实现 `packages/vue` 的编辑器 UI
- [x] 实现 `packages/vue` 的预览器 UI
- [x] 实现层级列表和属性面板
- [x] 联调编辑态与播放态
- [x] 将 `apps/playground` 从联调页重构为正式应用壳层
- [x] 实现项目列表页与正式工作台页
- [x] 实现本地项目持久化、模式切换和保存状态
- [x] 实现 timeline 基础配置 UI
- [x] 实现 JSON 导出与导入
- [x] P1：排查并修复工作台自适应布局问题，解决常见桌面视口下出现的非预期滚动条与内容溢出
- [x] 补齐编辑态步骤归属可视化：在默认全显前提下，为后续步骤元素提供明确提示
- [x] P1：修复编辑态仍按节点初始 `visible` 状态隐藏后续步骤对象的问题，确保编辑态默认展示当前 slide 的全部元素
- [x] P1：重构工作台信息架构与布局层级，减少过大的功能区和过多的平级分区，压缩非核心卡片体积，提升主编辑区与预览区的首屏占比
- [x] P1：重构工作台为三栏布局，左右侧栏固定，中间编辑区/预览区自适应，并支持左右侧栏一键收起与展开
- [x] P1：扩展时间轴步骤触发模型，支持页面点击、自动触发与对象点击，并为对象点击步骤补充触发对象选择能力
- [x] P1：调整工作台滚动交互策略，改为页面纵向滚动优先，移除主编辑区、预览区及其外层主容器中的非必要内嵌滚动
- [x] P1：精简工作台默认信息展示，合并或降级统计、摘要、次要状态和低频操作，减少页面模块数量与视觉干扰，突出主编辑区与预览区
- [x] P1：稳定编辑态拖拽 / 缩放后的单选态回写，避免对象变换后属性面板误回退到页面设置
- [x] VNext-00-M1 完成 `apps/playground` 项目列表页 Arco 化改造：接入 Arco 依赖、统一主题变量，并替换列表页中的按钮、输入、卡片、确认弹层和空状态组件
- [x] VNext-00-M2 完成 `apps/playground` 工作台页 Arco 化改造：替换顶栏输入、模式切换、动作按钮与状态反馈组件，并拆分工作台样式到独立文件以降低页面组件复杂度
- [x] VNext-00-M3 完成 `packages/vue` 预览壳层 Arco 化改造：替换预览壳层中的状态标签、操作按钮与信息卡片，并统一折叠区按钮为 Arco 组件
- [x] VNext-00-M4 完成 `packages/vue` 编辑壳层 Arco 化改造：统一工具栏与侧栏操作按钮、拆分样式文件并重构三栏视觉层级
- [x] VNext-00-M5 完成全局色彩与布局升级：统一 Arco 蓝灰主题 token，重构工作台顶部信息架构和统计卡片，同步编辑/预览与侧面板色调
- [x] VNext-00 基于 Arco Design Vue 重构 `apps/playground` 与 `packages/vue` 的页面壳层和通用 UI，统一布局、导航、表单、面板、弹窗与反馈组件风格
- [x] UI 视觉修正：移除全局白色网格背景覆盖层，保留纯渐变页面背景
- [x] UI 评审反馈：编辑器右侧“页面设置/组件属性/组件列表/时间轴”切换区改为 Arco Tabs 组件
- [x] UI 评审反馈：编辑器右侧 Tabs 从胶囊风格调整为简约线性风格
- [x] UI 评审反馈：移除左右侧栏原收起按钮，改为侧边角标控制展开/收起
- [x] UI 评审反馈：侧栏收起改为完全隐藏（不保留占位宽度），并清理编辑器中的英文界面文案
- [x] UI 评审反馈：移除工作台顶部模式摘要与“页面/对象/步骤”统计卡片区域
- [x] UI 评审反馈：移除编辑器工具栏“页面管理（新增页面/清空选择/删除选中）”区块
- [x] UI 评审反馈：移除左侧页面卡片中重复的“对象数/播放步骤数”文案
- [x] UI 评审反馈：重构工作台顶部工具区为紧凑双层布局，消除大面积留白并统一操作密度
- [x] UI 评审反馈：顶部“项目列表”改为返回图标，并重排模式/保存状态与操作按钮为单链路紧凑布局
- [x] UI 评审反馈：统一移除工作台、编辑器、预览器与列表页中重复介绍性质的蓝色小标题文案
- [x] UI 评审反馈：进一步压缩顶部高度、收窄左侧页面栏宽度，并精简时间轴面板的信息密度与视觉层级
- [x] UI 评审反馈：移除工作台外层固定最大宽度，改为随视口自适应铺满
- [x] UI 评审反馈：简化编辑器右侧“时间轴”Tab 内容，收敛空态文案、按需显示动作字段并折叠动画高级参数
- [x] UI 评审反馈：二次精简时间轴 Tab，移除步骤/动作/动画卡片头部与表单重复信息，仅保留单一配置入口
- [x] UI 评审反馈：参考截图重排工作台布局，扁平化顶栏与工具带、压缩左右侧栏占比并提升中间画布区视觉权重
- [x] UI 评审反馈：修复参考截图布局后右侧 Tabs 挤压导致“时间轴”标签不可见的问题，固定四个标签等宽展示并回补右栏宽度
- [x] UI 评审反馈：左右侧栏改为固定高度，超出内容在侧栏内部滚动，避免缩略页与属性面板继续撑高整页
- [x] UI 评审反馈：将动画设置迁移到“组件属性”Tab，并同步调整三栏留白、中间画布宽度自适应缩放与右侧 Tab 内容区扁平化样式
- [x] UI 评审反馈：工作台改为 100dvh 固定视口布局，按顶部工具区动态分配编辑器/预览器可用高度，移除页面级滚动条并将滚动收敛到内部区域
- [x] UI 评审反馈：移除右侧 Tabs 内重复的大标题层，仅保留分组标题与状态内容，降低视觉重复和首屏占用
- [x] UI 评审反馈：将右侧 Tab 内容中的胶囊背景状态文案改为简单文字标签，减少视觉噪音
- [x] UI 评审反馈：组件属性中将“播放归属”区块下移到最后，优先展示可直接编辑的基础属性与类型属性
- [x] UI 评审反馈：简化“组件列表”Tab 中的卡片信息层级，并通过更清晰的描边与选中态强化卡片边界
- [x] UI 评审反馈：增强“组件列表”Tab 卡片的 hover 与选中高亮，恢复更明确的边框、底色与阴影反馈
- [x] UI 评审反馈：恢复“时间轴”Tab 内分组、步骤与动作的卡片层级，让时间轴区域与组件列表保持一致的卡片化界面语言
- [x] UI 评审反馈：将工作台与编辑器中的原生控件全面替换为 Arco Vue 组件，统一按钮、输入框、数字框、选择器、开关、颜色选择器、折叠面板与上传控件的交互语言
- [x] UI 评审反馈：去除 Arco 控件上的旧自定义焦点/边框覆写，统一回到 Arco 默认风格，仅保留尺寸控制；同时修复 Select 弹层展开与组件属性编辑时误跳转到页面设置的问题
- [x] UI 评审反馈：为组件属性面板增加“最近一次稳定单选”保留机制，并移除 Arco Select 外层 label 容器，进一步修复属性编辑失焦导致的下拉闪退与误跳转
- [x] UI 评审反馈：修复时间轴 Tab 下拉选择器横向溢出，统一步骤与动作操作为文字按钮，并进一步简化时间轴卡片视觉层级
- [x] UI 评审反馈：优化编辑器左右侧栏收起角标样式，弱化厚重方块感并增强贴边控制感
- [x] UI 评审反馈：将左右侧栏收起按钮改为贴边半圆耳朵式 handle，并移出编辑画布区域
- [x] UI 评审反馈：右侧管理栏宽度小幅压缩，进一步让渡中间编辑区空间
- [x] UI 评审反馈：右侧管理栏进一步压缩至 300px，继续提升中间编辑区占比
- [x] UI 评审反馈：右侧窄栏中的下拉框在空间不足时自动隐藏当前文本，仅保留控件与箭头
- [x] UI 评审反馈：将顶部“插入”标题与新增动作合并为统一工具组，并改为文字按钮
- [x] UI 评审反馈：按容器宽高恢复编辑区画布等比缩放并保持居中完整显示，修正右侧下拉框文本误消失，并将动画新增/删除按钮改为 Arco Icon
- [x] UI 评审反馈：修正动画设置中“高级参数”折叠项的默认盒子样式，改为轻量文字折叠入口
- [x] UI 评审反馈：将动画“高级参数”从 Collapse 改为文字按钮切换的内联展开区，减少折叠容器样式干扰
- [x] UI 评审反馈：按编辑页标准统一优化预览页壳层、顶栏、三栏布局、侧栏收起控件与卡片层级
- [x] UI 评审反馈：继续细化预览页，补齐预览画布按容器完整缩放居中显示，并进一步压缩顶栏与侧栏信息密度
- [x] UI 评审反馈：预览页左侧页面列表改为更接近编辑页的缩略页卡片，右侧步骤卡片进一步轻量化
- [x] UI 评审反馈：继续压缩预览页顶栏与主画布区的视觉重量，减少重复状态标签并进一步突出画布主体
- [x] UI 评审反馈：进一步简化预览页左侧边栏，移除缩略图并改为更清晰的页面导航清单
- [x] UI 评审反馈：预览页左侧边栏改回与编辑页一致的缩略页卡片结构与间距
- [x] UI 评审反馈：进一步收窄预览页右侧步骤栏，并在收起时完全隐藏侧栏内容，仅保留侧边 handle
- [x] UI 评审反馈：移除预览页内嵌模式下重复的顶部/画布区播放摘要提示，避免同一信息多次出现
- [x] UI 评审反馈：预览页右侧步骤卡片改为固定高度，避免随侧栏容器剩余空间被拉伸
- [x] P1：修复 `VNext-01` 撤销 / 重做按钮在变更后仍保持禁用，确保新增对象、新增页面后可立即执行撤销/重做
- [x] P1：继续修复 `VNext-01` 撤销 / 重做按钮在变更后仍保持禁用，并在真实页面中完成撤销 / 重做闭环回归
- [x] `VNext-01` 产品 review 复测通过，撤销 / 重做基础入口已恢复可用
- [x] VNext-02 建立快捷键体系，支持删除、撤销、重做、复制、粘贴、重复、方向键微调和 `Shift + 方向键` 大步移动
- [x] VNext-03 实现节点复制 / 粘贴 / 重复，支持同页与跨 slide 粘贴，并保留基础属性、样式和资源引用
- [x] VNext-04 实现框选、多选、批量移动、对齐与分布能力，完成编辑器多选排版基础闭环
- [x] BUG-VNEXT04-01 修复多选批量操作历史记录拆分问题，确保批量移动 / 对齐 / 分布后，一次撤销或重做即可整体回退 / 重放本次批量操作
- [x] `VNext-04` 产品 review 复测通过，多选批量移动 / 对齐 / 分布与单次撤销 / 重做闭环已恢复
- [x] VNext-05 增强 slide 管理，支持复制、拖拽排序、删除确认、从当前页后快速新增下一页和 slide 命名
- [x] `VNext-05` 产品 review 测试通过，slide 复制、拖拽排序、删除确认、后插入、命名与保存恢复主链路验证通过
- [x] VNext-06 增强图层管理，支持图层重命名、锁定 / 解锁、隐藏 / 显示，以及列表中快速定位当前选中节点
- [x] `VNext-06` 产品 review 测试通过，图层重命名、锁定 / 解锁、预览隐藏 / 显示与快速定位当前选中节点主链路验证通过
- [x] VNext-07 时间轴步骤支持上移 / 下移排序，并通过标准命令层写回当前 slide
- [x] VNext-07 时间轴步骤支持复制，并保留原步骤的触发器与动作配置且以单次历史记录插入到原步骤后
- [x] VNext-07 支持从当前步骤切入预览模式，并让预览器先恢复该步骤之前的播放状态再继续播放
- [x] VNext-07 补齐工作台内嵌预览态的“一键重置当前 slide 播放状态”入口
- [x] `VNext-07` 产品 review 测试通过，步骤重命名、排序、复制、删除、从当前步骤开始预览、重置播放与动画 `duration / delay` 参数链路验证通过
- [x] VNext-08 实现本地图片导入，支持直接选择本地图片文件插入当前 slide，并随项目本地持久化后恢复
- [x] `VNext-08` 产品 review 测试通过，本地图片导入、自动保存、刷新恢复与预览渲染主链路验证通过
- [x] VNext-09 实现页面背景配置，支持按 slide 设置背景色、背景图和基础填充方式，并打通编辑态 / 预览态渲染与缩略图预览
- [x] `VNext-09` 产品 review 复测通过，新建页面 / 后插入背景初始化、快速切页、复制页面边界、缩略页、刷新恢复与预览渲染链路验证通过
- [x] VNext-10 项目创建与首页入口优化：已完成首页首屏精简、新建项目弹窗和项目创建时直设项目名称 / 页面尺寸的链路打通
- [x] `VNext-10` 产品 review 测试通过，首页首屏精简、新建项目弹窗、预设 / 自定义尺寸、创建后进入工作台与列表摘要回写链路验证通过
- [x] VNext-11 图片插入与替换链路升级：已统一工具栏“图片”入口命名，移除“图片 / 导入图片”重复表达，并在组件属性中支持直接更换当前图片
- [x] `VNext-11` 产品 review 测试通过，工具栏图片入口统一、属性面板换图、自动保存、刷新恢复与预览渲染链路验证通过
- [x] VNext-12 图片一键设为页面背景：已在工具栏图片上传流中补齐“设为背景”入口，并支持上传后直接写入当前 slide 背景图并切回页面设置
- [x] `VNext-12` 产品 review 测试通过，工具栏“设为背景”上传入口、页面设置回显、刷新恢复与预览背景渲染链路验证通过
- [x] VNext-13 编辑区快捷交互增强：已补齐编辑区右键菜单，并支持文本节点双击后直接进入画布内编辑态
- [x] `VNext-13` 产品 review 复测通过，编辑区右键菜单、文本双击画布内编辑、属性面板回写、本地持久化、刷新恢复与预览态链路验证通过
- [x] VNext-14 缩略图自动截图同步：已在保存与切页前补齐当前 slide 自动截图，并同步更新编辑/预览左侧缩略页封面与项目列表封面
- [x] `VNext-14` 产品 review 测试通过，缩略图自动截图同步、编辑/预览侧栏封面、项目列表封面与刷新恢复链路验证通过
- [x] VNext-15 组件管理面板重构：已将组件列表改为画布右上角独立浮层，支持拖拽排序改层级、外化高频动作，并将右侧“页面设置”改为按需打开的独立抽屉入口
- [x] UI 评审反馈：图层浮层默认收起，入口改为 Arco 图层 Icon，并调整为右上角紧凑工具位
- [x] UI 评审反馈：图层浮层列表样式进一步简化，收敛为更接近设计稿的单行轻量列表
- [x] UI 评审反馈：图层列表拖拽手柄缩小，文案改为“类型-名称”，并将右侧操作统一为眼睛与锁图标
- [x] UI 评审反馈：补齐 Arco Icon 插件注册，修复图层入口、类型图标与眼睛 / 锁图标不显示的问题
- [x] UI 评审反馈：移除左侧类型图标、放大右侧眼睛 / 锁图标，并先下掉图层浮层底部按钮组
- [x] UI 评审反馈：继续放大图层列表右侧眼睛 / 锁图标与点击热区，提升可见性与可点性
- [x] UI 评审反馈：页面设置入口改为 icon 按钮，右侧管理栏默认收起
- [x] UI 评审反馈：图层列表右侧眼睛 / 锁操作去除圆形按钮底，仅保留纯 icon 呈现
- [x] UI 评审反馈：图层列表左侧拖拽手柄移除虚线框包裹，仅保留纯 icon 呈现
- [x] `VNext-15` 产品 review 测试通过，图层浮层展开/收起、选中联动、拖拽改层级、显隐/锁定快捷操作、页面设置抽屉与刷新恢复链路验证通过
- [x] VNext-16 页面背景独立性修复：已补齐 `pnpm test:vnext-16` 自动化回归，验证新建页面、后插入、刷新恢复与预览态的背景独立链路通过
- [x] VNext-17 文本双击编辑写回修复：已确认对应修复由 `VNext-13` 缺陷修复落地，并在 2026-04-08 再次执行 `pnpm test:vnext-13` 通过，属性面板、本地持久化、刷新恢复与预览态保持一致
- [x] VNext-18 Text 缩放链路修复：已修复适配层未识别 Fabric `resizing` 动作导致文本缩放不落盘的问题，并通过 `pnpm test:vnext-18` 验证编辑态缩放、持久化、刷新恢复与预览态烟测
- [x] VNext-19 图片右键快捷设为背景：已在图片对象右键菜单中补齐“设为背景”入口，执行后会同步把当前图片节点转换为当前 slide 背景，并移除原图片节点与关联时间轴引用；已通过 `pnpm test:vnext-19` 与 `pnpm test:vnext-13` 回归
- [x] VNext-20 设为背景填充方式直选：已为“设为背景”确认流程补齐 `cover / contain / fill` 选择能力；当前保留图片右键“设为背景”入口，并通过 `pnpm test:vnext-20`、`pnpm test:vnext-12`、`pnpm test:vnext-16` 与 `pnpm test:vnext-19` 验证背景导入、填充方式写回与持久化恢复
- [x] UI 评审反馈：移除编辑器工具栏中的“设为背景”和“图片框”入口，工具栏插入区收敛为“文本 / 矩形 / 图片”，背景设置改由右键菜单与页面设置链路承接
- [x] UI 评审反馈：移除编辑区右键菜单中的“插入图片框”入口，空白区快捷创建能力收敛为“插入文本 / 插入矩形 / 插入图片”
- [x] VNext-21 换图后节点名称同步增强：已补齐图片资源切换时的默认节点名称 / 资源标识自动同步逻辑；保留手动重命名不被覆盖，并通过 `pnpm test:vnext-21` 与 `pnpm test:vnext-11` 验证换图、图层列表显示、自动保存与刷新恢复链路
- [x] VNext-22 时间轴引用一致性治理：已统一节点删除、批量删除与“设为背景”链路下的时间轴清理规则，删除节点时会同步剔除失效的对象触发、动作目标与动画 `targetId` 引用，并自动删除失去触发闭环或动作集合的空步骤
- [x] VNext-22-A 引用模型盘点：已梳理 `trigger / action / animation targetId` 三类节点引用，并在 `packages/core/src/reducer/node-removal.ts` 中沉淀统一清理矩阵
- [x] VNext-22-B 删除入口统一：已将单个删除、多选批量删除和图片转背景统一收口到 `packages/core` 的节点移除清理 helper，避免不同入口行为不一致
- [x] VNext-22-C 空步骤自动删除：当步骤失去有效 `node-click` 触发对象或全部动作被清空时，会自动从当前 slide 的 timeline 中移除步骤
- [x] VNext-22-D 一致性回归：已补齐 `pnpm test:vnext-22`，覆盖编辑态时间轴面板、存储数据与预览态播放结果的一致性验证
- [x] VNext-23 对象级右键菜单语义优化：已按空白区 / 单选对象 / 多选对象重整右键菜单显示规则；对象级菜单移除插入类操作，图片单选补齐“更换图片”快捷入口，并与“设为背景”统一为图片专属操作组
- [x] VNext-23-A 菜单上下文矩阵：已补齐右键菜单上下文载荷，明确空白区、单选对象、多选对象三类菜单的选中态来源与显示边界
- [x] VNext-23-B 对象级菜单收敛：已从对象级菜单中移除插入类操作，确保对象快捷操作与空白区创建能力彻底分流
- [x] VNext-23-C 图片专属快捷操作补齐：已为图片对象右键菜单补齐“更换图片”入口，并与“设为背景”统一排序和分组
- [x] VNext-23-D 菜单一致性回归：已补齐 `pnpm test:vnext-23`，并联动执行 `pnpm test:vnext-13`、`pnpm test:vnext-20` 与 `pnpm test:vnext-21` 验证右键菜单、图片换图与设背景链路一致性
- [x] BUG-2026-04-10 点击元素后画布收缩：已将编辑区缩放基准从 `clientWidth / clientHeight` 切换为稳定的 border-box 尺寸读取，避免选中 Fabric 对象时因临时滚动条预留或布局抖动触发整块画布误缩放；已补齐 `pnpm test:bug-canvas-scale-on-select` 高分屏回归
- [x] BUG-2026-04-10 浏览器缩放后侧栏收起按钮跑偏：已下调编辑器与预览器的三栏堆叠断点，并为真正单列场景补齐侧栏 handle 的安全定位；已通过 `pnpm test:bug-sidebar-handle-offset-on-zoom` 验证 960px 缩放场景下编辑/预览布局仍保持稳定
- [x] UI 评审反馈：工作台锁定最小 1280px 三栏基线，小于该宽度时改为横向滚动承载，不再继续压缩编辑器 / 预览器布局；已通过 `pnpm test:bug-sidebar-handle-offset-on-zoom` 验证 960px 视口下工作台实际宽度保持 1280px，编辑态展开右栏与预览态默认布局均维持三栏
- [x] 文档补充：已基于 `packages/core` 与 `packages/fabric` 真实代码结构在根目录 `README.md` 增加系统结构图、编辑态链路图与播放态链路图
- [x] 文档补充：已在根目录 `README.md` 增加 `packages/core` 与 `packages/fabric` 的树形文件图，并标注每个文件的职责
- [x] 工程治理：已在 `packages/core/src/commands.ts` 抽离统一的 `COMMAND_TYPES` 常量导出，并将 `packages/core` 与 `packages/vue` 中的 `command.type` 使用收口为常量引用
- [x] VNext-24 页面设置整体逻辑优化：已统一页面设置入口为“当前页设置”浮动入口，补齐当前页名称 / 页序 / 作用范围摘要，并将页面基础信息与背景配置拆为独立分组；“设为背景”快捷链路现已统一回跳到背景配置分组并展示成功反馈；已通过 `pnpm test:vnext-24`、`pnpm test:vnext-20` 与 `pnpm test:vnext-15`
- [x] VNext-24-A 页面设置入口与作用域提示：已统一页面设置入口形态，并在抽屉首屏补齐当前页上下文与“仅作用于当前页”的作用范围提示
- [x] VNext-24-B 页面字段分组重排：已将页面名称 / 尺寸收敛为“页面基础信息”组，并把背景色 / 背景图 / 填充方式 / 预览收敛为“背景配置”组
- [x] VNext-24-C 快捷链路回跳与回显统一：已统一“设为背景”等快捷链路执行后自动打开页面设置抽屉并聚焦背景配置组，同时展示统一成功反馈
- [x] VNext-24-D 页面设置信息减负：已将页面设置首屏压缩为页面摘要 + 两个折叠信息组，减少重复说明和低价值平铺信息
- [x] VNext-24-A1 页面设置入口统一：已统一页面设置入口 icon、按钮形态与抽屉打开方式，默认从同一“当前页设置”入口进入
- [x] VNext-24-A2 当前页作用域提示：已在页面设置中明确展示当前页名称、页序和“仅作用于当前页”的作用范围提示
- [x] VNext-24-B1 页面基础字段分组：已将页面名称、页面尺寸等基础字段收敛为页面基础信息组，并置于首屏优先展示
- [x] VNext-24-B2 背景配置分组：已将背景色、背景图、填充方式和背景预览收敛为背景配置组
- [x] VNext-24-C1 快捷链路回跳规则统一：已统一“设为背景”等快捷链路执行后自动打开页面设置并定位到背景配置组
- [x] VNext-24-C2 结果回显统一：已保证页面设置抽屉中的反馈、背景状态摘要与存储结果保持一致
- [x] VNext-24-D1 重复入口与重复文案清理：已收敛重复入口，仅保留统一的当前页设置入口，并移除抽屉内重复说明
- [x] VNext-24-D2 低频项收敛：已通过折叠分组收敛低频项，避免页面设置首屏继续平铺过多字段
- [x] 2026-04-10：按用户指令结束当前版本并完成版本归档，详见 `docs/RELEASES.md` 中的 `Release 05`
- [x] VNext-30 `TextTool` 浮动文本工具条：已在 `Text` 元素双击进入编辑态后，于元素上方显示就地工具条，支持颜色、字号、加粗、斜体；并与右侧属性面板、自动保存和刷新恢复保持一致，已通过 `pnpm typecheck`、`pnpm build`、`pnpm test:vnext-30` 与 `pnpm test:vnext-13`
- [x] VNext-31 轻量诊断日志能力：已补齐统一 logger、最近日志内存保留、开发态 `window.__CW_DIAGNOSTICS__` 查看桥接与控制台输出，并接入命令执行、自动保存、导入导出、图片资源处理、页面背景写入与时间轴关键变更日志；已通过 `pnpm typecheck`、`pnpm build` 与 `pnpm test:vnext-31`
- [x] VNext-31-A 统一 logger、结构化字段模型与最近日志内存保留
- [x] VNext-31-B 开发态查看入口与控制台输出
- [x] VNext-31-C 接入命令执行、页面背景写入与时间轴关键变更日志
- [x] VNext-31-D 接入自动保存、导入导出与图片资源处理日志
- [x] VNext-31-E 补齐自动化验证与开发自测，覆盖日志查看入口、自动保存、背景写入与时间轴变更

## 待完成

- 当前无待完成执行任务。

## 进行中

- 当前无进行中任务。

## 当前下一步

- `VNext-31` 已完成当前开发与自测，等待用户 review；若日志字段、展示方式或链路覆盖范围需要微调，再以本次实现为基础继续细化。
- 若下一轮继续扩展日志能力，可优先评估是否补充日志筛选、日志导出或更多失败链路接入。
- 若后续要继续扩展日志能力，请先从 `docs/REQUIREMENTS.md` 重新收集新诉求，再纳入新的版本规划。

## 相关文档

- 需求池见 `docs/REQUIREMENTS.md`
- 下一版本规划见 `docs/VNEXT.md`
- 已完成版本归档见 `docs/RELEASES.md`
