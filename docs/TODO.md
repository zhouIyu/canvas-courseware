# Canvas Courseware SDK MVP TODO

本文档只记录当前 MVP 的项目交付任务，不记录流程说明、协作约定或仓库规则。每完成一项，就立即更新一次状态。

## 已完成

- [x] 明确 MVP 基本范围：Text / Image / Rect、多页 slide、每页独立 canvas
- [x] 明确交付形态：TypeScript SDK + Vue 3 monorepo + Fabric MVP
- [x] 增加架构约束：UI 层与事件层解耦
- [x] 明确编辑态与预览态的元素展示策略：编辑态默认全显，预览态按 timeline 严格逐步展示
- [x] 定义通用 JSON schema 与事件协议
- [x] 设计 `Document / Slide / Node / Timeline / Animation` schema
- [x] 设计命令接口：新增、更新、删除、选中、层级调整、切换 slide
- [x] 设计并实现编辑态 store 与控制器
- [x] 实现 `packages/core` 的 schema / reducer / store / controller 源码
- [x] 搭建 monorepo 基础工程
- [x] 搭建 `apps/playground` 初始骨架
- [x] 实现 `packages/fabric` 的编辑态适配器
- [x] 实现 `packages/fabric` 的播放态适配器
- [x] 实现 `packages/vue` 的编辑器 UI
- [x] 实现 `packages/vue` 的预览器 UI
- [x] 实现层级列表和属性面板
- [x] 联调编辑态与播放态
- [x] 将 `apps/playground` 从联调页重构为正式应用壳层
- [x] 实现项目列表页与正式工作台页
- [x] 实现本地项目持久化、模式切换和保存状态
- [x] 实现 timeline 基础配置 UI
- [x] 实现 JSON 导出与导入
- [x] P1：排查并修复工作台自适应布局问题，解决常见桌面视口下出现的非预期滚动条与内容溢出
- [x] 补齐编辑态步骤归属可视化：在默认全显前提下，为后续步骤元素提供明确提示
- [x] P1：修复编辑态仍按节点初始 `visible` 状态隐藏后续步骤对象的问题，确保编辑态默认展示当前 slide 的全部元素
- [x] P1：重构工作台信息架构与布局层级，减少过大的功能区和过多的平级分区，压缩非核心卡片体积，提升主编辑区与预览区的首屏占比
- [x] P1：重构工作台为三栏布局，左右侧栏固定，中间编辑区/预览区自适应，并支持左右侧栏一键收起与展开
- [x] P1：扩展时间轴步骤触发模型，支持页面点击、自动触发与对象点击，并为对象点击步骤补充触发对象选择能力
- [x] P1：调整工作台滚动交互策略，改为页面纵向滚动优先，移除主编辑区、预览区及其外层主容器中的非必要内嵌滚动
- [x] P1：精简工作台默认信息展示，合并或降级统计、摘要、次要状态和低频操作，减少页面模块数量与视觉干扰，突出主编辑区与预览区
- [x] P1：稳定编辑态拖拽 / 缩放后的单选态回写，避免对象变换后属性面板误回退到页面设置


## 进行中

- [ ] 自测 MVP 主流程（Round 11 已完成对象点击触发闭环测试，当前未发现新缺陷；拖拽 / 缩放仍待继续覆盖）

## 待办

## 当前下一步

- 等待用户 review 本轮拖拽 / 缩放选中态稳定性修复；review 通过后，再继续后续版本开发。

## 下一版本待启动（VNext）

以下任务不纳入当前修复范围，待当前版本收口并通过 review 后再启动。

### 第一批：编辑效率基础

- [ ] VNext-00 基于 Arco Design Vue 重构 `apps/playground` 与 `packages/vue` 的页面壳层和通用 UI，统一布局、导航、表单、面板、弹窗与反馈组件风格
- [ ] VNext-01 实现撤销 / 重做，覆盖新增、删除、移动、缩放、属性修改、层级调整、slide 管理和时间轴编辑等主要操作
- [ ] VNext-02 建立快捷键体系，支持删除、撤销、重做、复制、粘贴、重复、方向键微调和 `Shift + 方向键` 大步移动
- [ ] VNext-03 实现节点复制 / 粘贴 / 重复，支持同页与跨 slide 粘贴，并保留基础属性、样式和资源引用
- [ ] VNext-04 实现框选、多选、批量移动、对齐与分布能力，提升页面排版效率
- [ ] VNext-05 增强 slide 管理，支持复制、拖拽排序、删除确认、从当前页后快速新增下一页和 slide 命名
- [ ] VNext-06 增强图层管理，支持图层重命名、锁定 / 解锁、隐藏 / 显示，以及列表中快速定位当前选中节点

### 第二批：编排与内容能力

- [ ] VNext-07 增强时间轴编排，支持步骤重命名、排序、复制、删除、从当前步骤开始预览、一键重置当前 slide 播放状态，并补齐 `duration` / `delay` 参数
- [ ] VNext-08 实现本地图片导入，支持直接选择本地文件并在项目持久化后恢复
- [ ] VNext-09 实现页面背景配置，支持按 slide 设置背景色、背景图和基础填充方式
